#!/bin/bash

# install Flat Assembler (for building all code)
sudo apt install fasm

# install mtools (for burning image file)
sudo apt install mtools

# install bochs (for running the OS)
sudo apt-get install bochs bochs-x